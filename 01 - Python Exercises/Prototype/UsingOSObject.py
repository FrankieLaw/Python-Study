import os

directory = os.listdir( "./" )  # Current Project Directory
directory = os.listdir( "/" )   # Root of your Hard Drive (C:/)
directory = os.listdir( "F:/" ) # Root of F:/ drive

print( directory )

